import { IOptions, ITransformOptions } from './typings';
declare class JSX2CSS {
    private type;
    constructor(options?: IOptions);
    transform(options: ITransformOptions): string;
}
export default JSX2CSS;
